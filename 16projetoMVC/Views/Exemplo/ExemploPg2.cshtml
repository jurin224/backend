<button class="btn btn-outline-info">Clique aqui</button>

@{   //o @ ele chama a funçao 
    string nome = "Julio";
}

<h1>Olá, @nome</h1>
<p>Hoje é @DateTime.Now.ToString("dd/MM/yyyy").</p> 
<p>Horario de Brasilia: @DateTime.Now.ToString("hh:mm").</p>

@{
    bool mostrarMensagem = true;
    if (mostrarMensagem == true)
    {
        <p> esta é uma mensagem que entrou no if</p>
    }

    for (var i = 1; 1 <= 10; i++)
   {
    int res = i*8;
    <p>8 x @i = @res </p>
   }
}